/*
Copyright 2024 New Vector Ltd.

SPDX-License-Identifier: AGPL-3.0-only OR GPL-3.0-only OR LicenseRef-Element-Commercial
Please see LICENSE files in the repository root for full details.
*/

.mx_BazaarPage {
    --bz-bg: var(--agrino-bg);
    --bz-panel: var(--agrino-surface);
    --bz-line: var(--agrino-surface-border);
    --bz-text: var(--cpd-color-text-primary);
    --bz-muted: var(--cpd-color-text-secondary);
    --bz-green: var(--agrino-accent);
    --bz-green-hover: #2a5428;
    --bz-green-soft: var(--agrino-accent-light);
    --bz-red: var(--cpd-color-text-critical-primary);
    --bz-orange: #f59e0b;
    --bz-shadow: 0 4px 16px rgba(50, 100, 48, 0.08);
    --bz-shadow-hover: 0 6px 20px rgba(50, 100, 48, 0.15);
    --bz-radius: 20px;
    --bz-hover-surface: var(--agrino-hover-bg);
    --bz-selected-surface: var(--agrino-selected-bg);
    --bz-subtle-bg: var(--agrino-surface-muted);
    --bz-input-bg: var(--cpd-color-form-bg);
    --bz-input-border: var(--cpd-color-form-border);
    --bz-secondary-btn-bg: var(--agrino-surface-muted);
    --bz-secondary-btn-text: var(--cpd-color-text-primary);

    display: flex;
    gap: 16px;
    padding: 16px;
    min-height: 100%;
    width: 100%;
    box-sizing: border-box;
    background: var(--bz-bg);
    color: var(--bz-text);
    overflow-y: auto;
    font-family: inherit;
}

.mx_BazaarPage_sidebar {
    width: 320px;
    flex-shrink: 0;
    background: var(--bz-panel);
    border: 1px solid var(--bz-line);
    border-radius: var(--bz-radius);
    box-shadow: var(--bz-shadow);
    overflow: hidden;
    align-self: flex-start;
    position: sticky;
    top: 0px;
    max-height: calc(100vh - 32px);
    display: flex;
    flex-direction: column;
}

.mx_BazaarPage_sidebarHeader {
    padding: 20px 18px;
    background: var(--bz-green);
    color: #ffffff;

    h1 {
        margin: 0;
        font-size: 22px;
        font-weight: 800;
        letter-spacing: -0.02em;
    }

    p {
        margin: 8px 0 0;
        font-size: 14px;
        opacity: 0.92;
        line-height: 1.5;
    }
}

.mx_BazaarPage_menu {
    overflow: auto;
    flex: 1;
}

.mx_BazaarPage_menuItem {
    border-bottom: 1px solid var(--bz-line);
}

.mx_BazaarPage_menuMain {
    width: 100%;
    border: none;
    background: var(--bz-panel);
    padding: 15px 18px;
    text-align: inherit;
    font-size: 16px;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: var(--bz-text);
    transition: background 0.2s ease;

    &:hover {
        background: var(--bz-hover-surface);
    }

    &.active {
        background: var(--bz-selected-surface);
        color: var(--bz-green);
        font-weight: 700;
    }
}

.mx_BazaarPage_chevron {
    transition: transform 0.2s;
}

.mx_BazaarPage_menuItem.open .mx_BazaarPage_chevron {
    transform: rotate(180deg);
}

.mx_BazaarPage_submenu {
    padding: 8px 12px 12px;
    background: var(--bz-subtle-bg);

    button {
        width: 100%;
        border: 1px solid var(--bz-line);
        background: var(--bz-panel);
        color: var(--bz-text);
        border-radius: 12px;
        padding: 10px 12px;
        margin: 6px 0;
        text-align: inherit;
        cursor: pointer;
        transition: 0.2s;
        font-size: 15px;

        &:hover {
            border-color: var(--bz-green);
            background: var(--bz-selected-surface);
        }

        &.selected {
            border-color: var(--bz-green);
            background: var(--bz-selected-surface);
            color: var(--bz-green);
            font-weight: 700;
        }
    }
}

.mx_BazaarPage_content {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 16px;
}

.mx_BazaarPage_topbar {
    background: var(--bz-panel);
    border: 1px solid var(--bz-line);
    border-radius: var(--bz-radius);
    box-shadow: var(--bz-shadow);
    padding: 20px 22px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
}

.mx_BazaarPage_selectedInfo {
    display: flex;
    flex-direction: column;
    gap: 6px;

    h2 {
        margin: 0;
        font-size: 22px;
        font-weight: 800;
        color: var(--bz-text);
        letter-spacing: -0.02em;
    }

    span {
        color: var(--bz-muted);
        font-size: 15px;
        line-height: 1.5;
    }
}

.mx_BazaarPage_actions {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
}

.mx_BazaarPage_btn {
    border: none;
    padding: 10px 18px;
    border-radius: 12px;
    cursor: pointer;
    font-size: 14px;
    font-weight: 600;
    transition: background 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
    color: #ffffff;
    white-space: nowrap;
    line-height: 1.4;

    &:hover:not(:disabled) {
        box-shadow: 0 3px 10px rgba(50, 100, 48, 0.2);
    }

    &:disabled {
        opacity: 0.6;
        cursor: not-allowed;
    }

    &.mx_BazaarPage_btn--buy,
    &.mx_BazaarPage_btn--sell {
        background: var(--bz-green);

        &:hover:not(:disabled) {
            background: var(--bz-green-hover);
        }
    }

    &.mx_BazaarPage_btn--secondary {
        background: var(--bz-secondary-btn-bg);
        color: var(--bz-secondary-btn-text);
        border: 1px solid var(--bz-line);
        box-shadow: none;

        &:hover:not(:disabled) {
            border-color: var(--bz-green);
            background: var(--bz-hover-surface);
            color: var(--bz-green);
            box-shadow: none;
        }
    }

    &.mx_BazaarPage_btn--danger {
        background: var(--bz-red);

        &:hover:not(:disabled) {
            box-shadow: 0 4px 12px rgba(220, 38, 38, 0.25);
        }
    }

    &.mx_BazaarPage_btn--warning {
        background: var(--bz-orange);
        color: #ffffff;

        &:hover:not(:disabled) {
            box-shadow: 0 4px 12px rgba(245, 158, 11, 0.25);
        }
    }
}

.mx_BazaarPage_panel {
    background: var(--bz-panel);
    border: 1px solid var(--bz-line);
    border-radius: var(--bz-radius);
    box-shadow: var(--bz-shadow);
    padding: 22px 24px;
    box-sizing: border-box;

    h3 {
        margin: 0 0 6px;
        font-size: 20px;
        font-weight: 800;
        color: var(--bz-text);
        letter-spacing: -0.02em;
    }

    h4 {
        margin: 0 0 10px;
        font-size: 18px;
        font-weight: 700;
        color: var(--bz-text);
    }
}

.mx_BazaarPage_sectionUnderline {
    width: 40px;
    height: 3px;
    background-color: var(--bz-green);
    border-radius: 2px;
    margin-bottom: 18px;
}

.mx_BazaarPage_grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
}

.mx_BazaarPage_field {
    display: flex;
    flex-direction: column;
    gap: 6px;

    label {
        font-size: 14px;
        font-weight: 600;
        color: var(--bz-muted);
    }

    input,
    select,
    textarea {
        width: 100%;
        border: 1px solid var(--bz-input-border);
        border-radius: 12px;
        padding: 12px 14px;
        font-size: 15px;
        background: var(--bz-input-bg);
        outline: none;
        font-family: inherit;
        color: var(--bz-text);
        box-sizing: border-box;

        &:focus {
            border-color: var(--bz-green);
            box-shadow: 0 0 0 3px rgba(50, 100, 48, 0.12);
        }
    }

    textarea {
        min-height: 90px;
        resize: vertical;
    }
}

/* Element's global reset in _common.pcss uses a 3×:not() + descendant selector
   which resolves to specificity 0,0,4,1 — higher than any reasonable chain of
   our own classes. Use !important on the properties it steals (background,
   padding, font) rather than escalating specificity further. */
.mx_BazaarPage_field input[type="text"],
.mx_BazaarPage_field input[type="password"],
.mx_BazaarPage_field input[type="search"] {
    background: var(--bz-input-bg) !important;
    color: var(--bz-text) !important;
    padding: 12px 14px !important;
    font: inherit !important;
    font-size: 15px !important;
    font-weight: normal !important;
    border: 1px solid var(--bz-input-border) !important;
    border-radius: 12px !important;
    width: 100% !important;
    box-sizing: border-box !important;
}

.mx_BazaarPage_checkboxGroup {
    display: flex;
    flex-direction: column;
    gap: 8px;
    padding: 8px 0;
}

.mx_BazaarPage_checkboxItem {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 15px;
    color: var(--bz-text);
    cursor: pointer;

    input {
        width: auto;
        margin: 0;
    }
}

.mx_BazaarPage_sectionActions {
    display: flex;
    gap: 10px;
    margin-top: 16px;
    flex-wrap: wrap;
}

.mx_BazaarPage_filters {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 12px;
    margin-bottom: 14px;
    align-items: end;
}

.mx_BazaarPage_filterButtons {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    align-self: end;
}

.mx_BazaarPage_cardList {
    display: grid;
    gap: 12px;
}

.mx_BazaarPage_adCard {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 16px;
    align-items: center;
    border: 1px solid var(--bz-line);
    border-radius: 16px;
    padding: 16px 18px;
    background: var(--bz-panel);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    box-sizing: border-box;

    &:hover {
        border-color: var(--bz-green);
        box-shadow: var(--bz-shadow-hover);
    }
}

.mx_BazaarPage_adMeta {
    h4 {
        margin: 0 0 8px;
        font-size: 18px;
        font-weight: 700;
        color: var(--bz-text);
    }

    p {
        margin: 0;
        color: var(--bz-muted);
        font-size: 15px;
        line-height: 1.75;
    }
}

.mx_BazaarPage_adPrice {
    display: inline-block;
    margin-top: 6px;
    font-size: 16px;
    font-weight: 700;
    color: var(--bz-green);
}

.mx_BazaarPage_adActions {
    display: flex;
    flex-direction: column;
    gap: 6px;

    button {
        min-width: 88px;
    }
}

.mx_BazaarPage_buyFlow {
    display: grid;
    gap: 14px;
}

.mx_BazaarPage_step {
    border: 1px solid var(--bz-line);
    border-radius: 16px;
    padding: 20px 22px;
    background: var(--bz-panel);

    p {
        margin: 8px 0;
        color: var(--bz-text);
        font-size: 16px;
        line-height: 1.6;
    }
}

.mx_BazaarPage_step--actions {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 12px;
}

.mx_BazaarPage_stepTitle {
    font-weight: 800;
    font-size: 17px;
    margin-bottom: 12px;
    color: var(--bz-green);
}

.mx_BazaarPage_detailHighlight {
    font-size: 18px;
    font-weight: 700;
    color: var(--bz-text);
}

.mx_BazaarPage_paymentPlaceholder {
    width: 100%;
    height: 200px;
    border-radius: 14px;
    margin: 14px 0;
    background:
        linear-gradient(135deg, rgba(50, 100, 48, 0.06), rgba(50, 100, 48, 0.1)),
        repeating-linear-gradient(
            45deg,
            var(--bz-subtle-bg),
            var(--bz-subtle-bg) 10px,
            var(--bz-line) 10px,
            var(--bz-line) 20px
        );
    border: 1px dashed var(--bz-green);
}

.mx_BazaarPage_myAds,
.mx_BazaarPage_myPurchases {
    display: grid;
    gap: 10px;
}

.mx_BazaarPage_myAd {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 14px;
    border: 1px solid var(--bz-line);
    border-radius: 14px;
    padding: 16px 18px;
    background: var(--bz-panel);
    flex-wrap: wrap;
    transition: border-color 0.2s ease;
    box-sizing: border-box;

    &:hover {
        border-color: var(--bz-green);
    }

    strong {
        color: var(--bz-text);
        font-size: 17px;
        font-weight: 700;
    }

    small {
        color: var(--bz-muted);
        font-size: 14px;
        line-height: 1.6;
    }
}

.mx_BazaarPage_adRowActions {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
}

.mx_BazaarPage_tabButtons {
    display: flex;
    gap: 8px;
    margin-bottom: 18px;
    flex-wrap: wrap;
    padding: 4px;
    background: var(--bz-subtle-bg);
    border-radius: 12px;
    width: fit-content;

    .mx_BazaarPage_btn {
        border-radius: 8px;
    }

    .mx_BazaarPage_btn.active {
        background: var(--bz-green);
        color: #ffffff;
        border-color: transparent;
    }
}

.mx_BazaarPage_empty {
    color: var(--bz-muted);
    padding: 20px;
    font-size: 15px;
    text-align: center;
    background: var(--bz-subtle-bg);
    border-radius: 12px;
    border: 1px dashed var(--bz-line);
}

.mx_BazaarPage_badge {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 8px;
    font-size: 12px;
    font-weight: 600;
}

.mx_BazaarPage_badge--sold {
    background: rgba(220, 38, 38, 0.15);
    color: var(--bz-red);
}

.mx_BazaarPage_badge--active {
    background: var(--bz-green-soft);
    color: var(--bz-green);
}

.mx_BazaarPage_myAdInfo {
    display: flex;
    flex-direction: column;
    gap: 6px;
}

.mx_BazaarPage_myAdMeta {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
}

.mx_BazaarPage_previousAds {
    margin-top: 32px;
    padding-top: 24px;
    border-top: 1px solid var(--bz-line);
}

.mx_BazaarPage_previousAds h3 {
    margin-bottom: 16px;
    color: var(--bz-text);
}

.mx_BazaarPage_editPanel {
    margin-top: 20px;
    padding: 22px;
    border-radius: 14px;
    border: 1px solid var(--bz-line);
    background: var(--bz-subtle-bg);
    width: 100%;
    box-sizing: border-box;
}

.mx_BazaarPage_editPanel h3 {
    margin: 0 0 18px;
    font-size: 18px;
    font-weight: 700;
    color: var(--bz-green);
}

.mx_BazaarPage_editGrid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 18px;
}

.mx_BazaarPage_editField {
    display: flex;
    flex-direction: column;
    gap: 6px;
}

.mx_BazaarPage_editField label {
    font-size: 14px;
    font-weight: 600;
    color: var(--bz-muted);
}

.mx_BazaarPage_editField input {
    height: 44px;
    padding: 0 14px;
    border-radius: 10px;
    border: 1px solid var(--bz-input-border);
    background: var(--bz-input-bg);
    color: var(--bz-text);
    font-size: 15px;
}

.mx_BazaarPage_editActions {
    display: flex;
    justify-content: flex-end;
    gap: 12px;
    margin-top: 24px;
}

@media (max-width: 1100px) {
    .mx_BazaarPage {
        flex-direction: column;
    }

    .mx_BazaarPage_sidebar {
        width: 100%;
        position: relative;
        top: auto;
        max-height: none;
    }

    .mx_BazaarPage_filters {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }
}

@media (max-width: 720px) {
    .mx_BazaarPage_grid {
        grid-template-columns: 1fr;
    }

    .mx_BazaarPage_adCard {
        grid-template-columns: 1fr;

        img {
            width: 100%;
            height: auto;
        }
    }

    .mx_BazaarPage_adActions {
        flex-direction: row;
        flex-wrap: wrap;
    }

    .mx_BazaarPage_filters {
        grid-template-columns: 1fr;
    }
}
